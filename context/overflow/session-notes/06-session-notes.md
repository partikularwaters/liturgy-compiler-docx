# Progress Tracker Archive — Stabilization, Library Tooling, emil-design-eng Charter, and Track A/B Delivery

Session-by-session build narrative archived from `context/progress-tracker.md`'s
Session Notes on 2026-09-02 per Banka Protocol §2.9 (the live file's Session
Notes section had crossed ~2,000 words, reaching ~6,800). Covers: the 2026-08-15/16
integrity-stabilization phases, the 2026-08-24/25 post-adoption backlog and BA-007
closure, the 2026-08-26 port-collision/webapp-audit/contribution-policy fixes, the
2026-08-27 Library pull tooling and BA-006 fix, the full 2026-08-27→28
emil-design-eng motion charter (Phases 0-5, now closed), and the 2026-08-30→09-01
Track A/Track B delivery through the Reader/Library correction pass. Every settled
fact from this narrative still lives in Decisions Made and the Completed section
of the live file in full; this is the underlying session-by-session story, read on
demand via `grep`, never loaded wholesale by `/remember restore`. The n8n Liturgy
Automation thread was deliberately kept in the live file's own Session Notes
instead of archived here, even where an individual dated entry's own work was
settled — Ticket 8 (workflow finalization) and Ticket 9 (UptimeRobot) remain open,
and splitting a still-active initiative's narrative across two files risked losing
continuity for whoever picks it up next.

---

**2026-09-01 — Survey pairing findings resolved**
- Formula pairing candidates now require the same Formula kind as well as the already-required Section and opposite language. Pairing reads and every cleanup/update write now return a visible failure rather than silently reporting success; new Formula, Prayer, and Song creation tells the user that the library row was saved but its pair must be retried from edit if the pair write fails. Added coverage for Formula kind mismatch and failed pairing cleanup/companion lookup. Verification: `tsc --noEmit`, `npm test` (24 files, 109 tests), lint (0 errors; the same two pre-existing warnings), and `git diff --check`.

**2026-09-01 — Reader/Library correction pass completed after approved charter**
- Compile-to-Reader Scripture additions now keep their resolved Liturgy/Section target visibly locked; targetless Library additions use a renamed **+ New Scripture** path into the Reader with an explicit Back to Library context. The Reader’s sidebar now has its own bounded desktop scroll area.
- Removed the Library-side Scripture Section picker, made Library authoring modals immune to accidental backdrop dismissal, and created the distinct New Prayer Guide path instead of exposing guide state within the Prayer form. Empty Scripture copy and explicit return labels were also updated.
- Translation pairing is now reconciled on edits: Song pairs require matching kind and opposite language but deliberately ignore Section tags; Formula and Prayer pairs also require matching Section and their applicable type (Formula kind; Prayer guide status). Invalid stored pairs are severed. Scriptures retain their citation-based companion model rather than a stored translation pair.
- Mark editors always expose Clear when marking tools are present, and suppress only an empty preview. Sermon validation now requires Title, Passage, and Preacher (Series optional); saved sermons use small-caps titles and the agreed centered presentation across Compile/DOCX/Web responsive contexts. Required non-auth authoring fields now carry asterisks.
- Verification for this correction pass: `npx tsc --noEmit`, `npm test` (23 files, 107 tests), and `git diff --check` pass. `npm run lint` reports no errors and two pre-existing warnings (`app/page.tsx` image LCP and unused `kind` in `markableSections.ts`). The whole Track B worktree remains uncommitted; preserve known unrelated local artifacts when preparing a later commit.

**2026-08-31 — Track B chartered, delegated, and all 13 Junior-safe tickets built directly in this session**
- Full `charter` session settled six items as direct-build ("quick" — 2 bugs, 2 design-consistency fixes, notification badges) plus three genuine open questions that grew into a much larger redesign thread through discussion: Sermon's data model (Title/Series/Preacher fields added to the existing Passage), Selection natural-flow merging (a new per-instance toggle on exactly Righteousness of God/Call to Confession/The Lord's Discourses; Assurance of Pardon's old unconditional merge left untouched), and unifying the three "add a Scripture Selection" entry points around the Reader's existing target picker — which itself expanded into Song's Section-tagging model, Prayer's kind-to-Compile-View move, and a new Library add-modal replacing four `/new` pages. Full decision-by-decision record lives in this conversation's charter transcript, not duplicated here; the resulting plan was split via `delegate` into `context/delegation-queue.md`'s Track B section — 13 Junior-safe tickets (10-22) and 4 Senior-required tickets (23-26, the Song/Prayer/Formula/Library-modal thread, since each establishes a genuinely new pattern or changes an existing field's meaning).
- The decision was made to skip fresh-session/worktree delegation and have all tickets built directly in this same coordinator session instead — token cost is dominated by fresh-session context-reload overhead, not by parallelism, so this was the more efficient path for a batch this size. All 13 Junior-safe tickets were built and `survey`-verified one at a time, live-tested in a real browser against local Supabase (not just typechecked) — see `delegation-queue.md` for each ticket's specific verification evidence (real docx exports grepped for content, real DB rows queried to confirm saves, etc.).
- **Real bug caught and fixed during Ticket 11** (not shipped): the ticket's own literal instruction ("keep the surrounding `<p>`") would have produced invalid nested `<p>` tags, since `MarkedText` already renders its own `<p>`. Caught before verification, fixed to match every other `MarkedText` call site's convention instead.
- **`npm run db:reset` (run for Ticket 20's migration) wipes local Supabase data**, including the local test Curator account and sample liturgies that existed at session start (their origin predates this session — not from any seed script, `db:seed` is disabled in `supabase/config.toml`). Expected `db:reset` behavior (migrations only, no data), not a bug, but worth knowing: local dev data needs to be recreated by hand after any `db:reset`, unlike a real seed file. Production entirely unaffected.
- **(continued) Tickets 23-26 (the Senior-required batch) also built directly in this session, same session**, executed one at a time with a per-ticket "proceed here" confirmation:
  - **Ticket 23** (Formula `item_types`): found mid-execution that `item_types` is completely absent on a fresh local database (Feature 23, 2026-07-16, only ever set it live in Production, never in a tracked migration) — flagged before proceeding; confirmed to do the full reconstruction (all 18 Morning + 19 Vesper Sections, from `redesign-plan-v1.1.md`'s §Y table, cross-checked against live Section names/order first) rather than a narrow 3-cell patch, closing the gap for real. New migration `20260831020000_section_item_types.sql` flags in its own comment that it should be diffed against Production's real live values before applying there.
  - **Ticket 24** (Song multi-Section tagging): new `song_section_tags` join table. Duplicate-merge deliberately **not** automated — `songs` has no `created_at` and attribution/notes could genuinely differ between same-title rows, so blind deletion risked real data loss; the migration documents a review query instead, per the ticket's own pre-approved fallback for exactly this case. Found and fixed a real gap beyond the ticket's stated scope: neither `createSong` nor `updateSong` wrote to the new tags table at all, which would have made every newly created Song invisible to every Section's tag-filtered picker.
  - **Ticket 25** (Prayer kind → per-placement toggle): found that `PrayerItem.leaderOnly` already existed as a snapshot field (added 2026-08-25) — no new type field was needed, only a new policy (`prayerKindPolicy.ts`) driving its initial value and a new way to change it after placement. `Prayer.kind` removed from the type/form/actions entirely; the DB column itself is untouched (no migration), its own `default 'leader'` covers the omitted insert.
  - **Ticket 26** (Library add-modal): replaced `/songs/new`, `/prayers/new`, `/formulas/new` with one shared modal. Upgraded Song's Section field from single-select to a real multi-select (`sectionNames: string[]` through `songActions.ts`) — this was the actual point of the charter's "Sections, not just one" requirement, and closes Ticket 24's own flagged "replace-on-edit collapses other tags" limitation. Caught and fixed a related bug in the same pass: `AddSongPanel.tsx`'s incidental-edit-while-placing path was about to collapse a Song's full tag set down to just one Section; fixed to preserve the existing set.
- **`survey` ran formally against the Junior-safe batch (10-22)** — see this file's Up Next entry above for the two findings (both Minor, documentation-currency: stale `ui-registry.md`, and the new notification badge not matching the declared Badge token spec). The Senior-required batch (23-26) and a survey of the whole Track B diff together are still pending.
- End-of-session state: `npm test` (101/101), `tsc --noEmit`, `eslint`, `next build`, and `db:verify-contract` (7/7) all clean across the full 17-ticket batch. 59 files changed, all uncommitted.

**2026-08-30 — LiturgyDateRow Track A closed after post-ship review**
- Finished the responsive Lord's Day/service-row treatment and the delete-dialog presentation follow-ups after several direct visual checks. Final `survey` passed all three layers with no findings, then `moor` captured the reusable Modal/delete-confirmation contract in `ui-registry.md`.
- The complete Track A batch is verified but still uncommitted. The proposed single commit is “Polish liturgy history rows and deletion flow”; keep the known unrelated local Supabase/Next-generated changes outside it.
- Next application work is Track B, beginning with what the liturgy summary should display now that Sermon is incomplete and the current date repeats the Lord's Day identity; settle that content decision before changing row density.

**2026-08-30 (continued, new session after a Codex handoff) — Track A committed, one real commit-message correction**
- Restored mid-handoff: the prior session's own message said its Codex session "finished abruptly," Track A was already complete and verified on disk but genuinely uncommitted. Verified rather than trusted — re-ran `tsc --noEmit`, `eslint`, `npm test` (101/101), and a full production build before touching git, and read every changed file's diff directly rather than assuming the handoff note was accurate.
- Committed as one batch, correctly excluding `next-env.d.ts`/`supabase/config.toml` (known pre-existing drift) and a newly-appeared `supabase/snippets/Untitled query 270.sql` (a local Supabase Studio scratch artifact from Codex's own session, not source) — caught by reading `git status` rather than blindly staging everything changed.
- **Real correction, worth remembering:** the first commit message violated `CONTRIBUTING.md`'s own explicit rule against narrating session activity ("Follow-up to the liturgy row redesign, closing out the direct feedback given after seeing it live...") — the exact "Not this" pattern the file's own example warns against, even though the rule was already written down. This was caught, with a report requested before amending, not just a fix. Rewritten to describe the change from the author's perspective with no session/reviewer narration. One explicit exception: "moored to ui-tokens.md/etc." stays — that phrasing is established practice, not a violation, despite reading similarly to internal-tooling jargon at first glance. Two amends total (one to add the model name to the Codex trailer, one for the narration fix), then pushed as `cee123a`. **Lesson for future commits in this repo:** re-read a drafted commit message against `CONTRIBUTING.md`'s own before/after example specifically, not just generally — the failure mode is subtle enough to slip through even with the rule in hand, and "moored"-style tooling verbs are fine here, narrating *who reported what, when* is not.

**2026-08-28 (continued once more) — a fifth spacing bug, and a real proactive catch**
- One more spacing issue was reported: Song/item titles that wrap below their Section name (not inline) still read as too far away, even after the earlier Section-boundary contrast fix — a different gap than the one already fixed. Traced to the Section-title-plus-header row's own internal `gap-4` doing double duty for both its inline and wrapped cases; split into `gap-x-4`/`gap-y-1` rather than picking one compromise value.
- `survey`-reviewed the fix on request and, before reporting a clean pass, checked whether the same structural pattern exists elsewhere rather than only checking the one file changed — found `SectionCard.tsx` (Compile View) has the identical row shape, meaning the same bug almost certainly exists there too even though it hadn't been reported (likely just less noticeable given the surrounding edit-icon chrome). Flagged it as a finding rather than silently fixing it, given the earlier explicit "Public view link" scoping for the original batch of four fixes — extending the fix was then confirmed.
- `moor`-captured the shared `gap-x-4`/`gap-y-1` pattern once both fixes landed, so a future Section-title-plus-header row anywhere in the app starts from the right default instead of the single-`gap-4` mistake repeating a third time.

**2026-08-28 (continued further) — delegation-queue staleness fix, four Web View bugs, a real bilingual feature, and a mid-fix correction**
- A request came in to verify `delegation-queue.md` for staleness (found and fixed — all 9 tickets showed unchecked despite 1-7 being long complete) and moor any un-moored decisions (the confirmed `opengraph-image.tsx` routing bug, into `library-docs.md`). `survey`-reviewed both fixes together and caught a real date error in my own delegation-queue fix (wrong dates carried over from an unrelated migration) before it shipped.
- Confirmed, unprompted, that the OG image work is real and live: verified the actual `og:image` route returns a real PNG with correct headers.
- A question then came up on whether n8n's link-preview needs any API/token at all for *manual* sharing — clarified it doesn't (Facebook's own crawler reads `og:` tags from any pasted link regardless of who shares it), a genuinely useful interim path while Facebook App Review is pending.
- Reported the "Lord's Day" straight-apostrophe finding, confirmed it was already a documented rule (`code-standards.md`) being violated systemically, not a missing convention — fixed everywhere, not just the one new file.
- Four direct-observation bugs were reported on the public Web View: date format, a hardcoded Tagalog-only rubric with a real English liturgy now needing it, and spacing that made Sections and their own content look the same. Corrected my own scope assumption mid-conversation for the rubric (a Compile-View-only toggle, Web View purely reflective, not a runtime display toggle) before building anything.
- Got the actual English translation directly ("It's my own translation... should now stand in equal authority with what we have in Tagalog") rather than fabricating or paraphrasing it, per this project's own standing discipline around liturgical text.
- `survey`-reviewed the first three fixes proactively; found 2 real gaps (DOCX export unverified, interactive toggle unverified) and closed both with live evidence once a sign-in happened mid-session — extracted real DOCX XML for both languages, and did a real click-through-and-persist test of the Compile View toggle via the Browser tool.
- The spacing fix needed a real correction: the first pass was token-compliant (`ui-tokens.md`'s existing `gap-4`/`gap-6`) but it was caught, via a real screenshot, that it still wasn't visually distinct enough once checked against dense live content — re-fixed by widening contrast at both ends (tighter internal spacing, a new `gap-8` token for the Web View specifically) rather than just nudging one number, and verified live via multiple real Section-boundary screenshots this time, not just class-name inspection.
- Confirmed directly, since it was asked outright: no SQL was ever run against the database except what was explicitly handed over to paste manually — a direct, honest accounting of exactly which of the two legitimate write paths (the automation API, or manual SQL) was used for each change this session.

**2026-08-28 — emil-design-eng charter, Phase 5 (public Web View & Homepage), charter closed**
- Restored via `/remember restore`; confirmed no drift (`main` at `9474870`, matching the tracker's last Completed entry). This session was scoped as purely UI/UX, proceeding straight to Phase 5.
- Presented the Before/After/Why table for all seven planned changes (see the Completed entry above for the full built list); confirmed, then built all seven in one pass.
- Live-browser verification was initially blocked by an unrelated concurrent session: the one available local dev server (port 3555, already running) held an uncommitted, syntax-erroring `app/liturgy/[id]/view/og/route.ts` with literal placeholder text (`ROUTE HANDLER TEST`), which fails the whole Next.js dev compile project-wide. Left the other session's file untouched rather than fixing or restarting its process; fell back to static verification only (`tsc`/`eslint`/`npm test`) for the build-and-survey pass, matching this project's own established precedent for an external blocker (e.g. the 2026-08-25 `db:start` outage entries).
- `survey` caught one real Important issue on the first pass — the tooltip entrance's `origin-top` didn't track its actual `left`/`right` anchor side, violating the established "popover scales from its trigger" rule other dropdowns already follow. Fixed to `origin-top-left`/`origin-top-right`, re-ran survey clean.
- **Drift caught at `/remember save` time, not assumed:** the concurrent session's own fix landed as commit `e0f41f6` ("Move the Web View's og:image off Next's broken opengraph-image.tsx convention," a real Next.js/Turbopack routing bug — `opengraph-image.tsx` 404s specifically when nested as `[dynamic-segment]/static-folder/opengraph-image.tsx`, fixed by moving to a plain Route Handler) mid-session, after this session's own build/survey pass but before save. That resolved the dev-compile blocker, so a fresh local dev server was started and live verification completed after all: homepage screenshot confirmed correct rendering and anonymous-visitor gating (no Create Liturgy CTA), and `getComputedStyle` confirmed the entrance wrapper (`opacity, transform` / `0.25s` / `--ease-out-strong`) and the Browse Library CTA (`background-color, transform` / `0.16s` / `--ease-out-strong`) both carry exactly the built transition recipe. Server stopped afterward.
- This closes every phase (0-5) of the emil-design-eng charter opened 2026-08-27.

**2026-08-27 — emil-design-eng charter and Phase 0 (Motion Foundations)**
- Restored via `/remember restore`; found and corrected real drift (the prior save described the n8n automation's Tickets 1/2/3/5/6/7 as uncommitted and Ticket 4 as unbuilt, but `git log` showed the full ticket-partitioned sequence already on `origin/main` — see the Ticket 4 Completed entry and the standalone correction commit).
- A `charter` covering the whole UI/UX experience through the newly-installed `emil-design-eng` skill was then requested, done in phases. Aligned language (DOM-rendered surfaces only, excluding non-animatable DOCX/PDF export) and flagged the scope-expansion/stabilize-in-place tension before proceeding — proceeded, treating it as acceptable additive work.
- Worked through phase-order, token-location, and Phase-0-scope decisions (all three "Recommended" options chosen) — see Decisions Made.
- Built Phase 0: Motion sections in `ui-tokens.md`/`ui-rules.md`, no component changes. Next session should start Phase 1 (global chrome & primitives audit).

**2026-08-27 — Homepage "Recent Liturgies" date-pairing fix**
- Reported mid-session: Vesper and Morning of the same Lord's Day showed as separate entries on the Homepage. Diagnosed by comparing the live Homepage against `/liturgies` directly in the browser — confirmed `/liturgies` already pairs them correctly and the Homepage's widget was simply never migrated to that pattern when it shipped 2026-07-29.
- Confirmed the fix approach and the "5 dates, up to 10 liturgies" semantics before building. Full detail in the Completed entry above.

**2026-08-27 — First real Library pull executed**
- After building the pull/restore tooling, the actual pull was run against Production. First attempt failed with the script's own "missing env var" error — the single-line `VAR=... VAR=... npm run ...` form didn't carry through as pasted; switched to two separate `export` lines, which worked.
- Also surfaced along the way: `SUPABASE_SERVICE_ROLE_KEY` is a write-only "Sensitive" Vercel variable (consistent with the 2026-08-15 environment audit noted elsewhere in this file) — it couldn't be read back from Vercel's dashboard at all. Got both the URL and the service-role key from Supabase's own dashboard (Settings → API) instead, which is the actual source of truth for both values regardless of what Vercel has cached.
- Real pull: 7 Formulas, 12 Prayers, 26 Songs, 180 Scripture Selections — all shared/canonical (`owner_id is null`), no personal library entries or user data touched. Restored into local Supabase and spot-checked live. Full detail in the Completed entry above.
- Noted, not acted on: an untracked `docs/N8N-LITURGY-AUTOMATION-CHARTER-DRAFT.md` appeared on disk during this session, not created by this session's work — left alone, same treatment as the pre-existing `supabase/config.toml` drift.

**2026-08-27 — One-way Library pull/restore tooling**
- Prompted by the question of whether local Supabase is a duplicate of Production or just a testing skeleton — checked directly: real Bible text and schema, but zero real Liturgy/Formula/Prayer/Song content, since those have never had seed data (real content only ever entered live via the app).
- Discussed risk of a full duplicate (real PII in `auth.users`, directional risk of a `push` accidentally hitting Production) versus a narrower pull; agreed to a Library-only, shared/canonical-only, strictly one-way pull-then-restore workflow rather than a full mirror.
- Built and live-verified both scripts against local Supabase with synthetic data (both direction-guards, the `paired_id` two-pass restore). Full detail in the Completed entry above.

**2026-08-27 — BA-006 fixed**
- Restored via `/remember restore`; confirmed no drift beyond the already-known, already-flagged `supabase/config.toml` `experimental.pgdelta` toggle (left untouched, out of this commit, same as the prior session's note).
- Started on the next Up Next item after the restore was confirmed. Root-caused the effect as a pure prop-derived-state reset, which is exactly what a `key`-based remount replaces — no workaround or suppression comment needed. Full detail in the Completed entry above.
- Live-verified with local Supabase started fresh for the session and stopped again afterward, matching the established local-dev workflow.

**2026-08-26 — Amen Rule "Affirmation of Faith" residue check closed**
- Last open item from this session's punch list. Drafted the join query needed to check Production directly (`section_items` has no Section name of its own — resolved via `sections` → `liturgies` → `templates`); it was run against Production directly, same established pattern as prior sessions' direct-SQL checks. Zero rows — the risk documented in Known Issues since the 2026-08-25 Amen Rule revision never materialized. Closed with no code change.

**2026-08-26 — Webapp-audit gaps #4/#5 fixed (Reader → Library, anonymous exposure)**
- Continued the "resolve open issues" session after the port-collision fix (previous Session Notes entry), now that local Supabase actually works for live verification.
- Confirmed via a `charter`-style decision check: extend the Reader's real verse-marking flow into a "Scripture Library" mode (Recommended option), rather than the lighter prefill-the-manual-form alternative — citation/text for a Library save now comes from actually marking verses, matching how a Section-targeted Selection is built today.
- Generalized `ReaderClient.tsx`'s `targetSection`-only logic to a `hasTarget`/`canBuildSelection` pair covering both a Liturgy Section and a Library Section tag, and gated the whole Selection-building UI (not just the save action) behind sign-in. Full detail in the Completed entry above.
- Verified live: created a throwaway local Compiler account (`test-compiler@example.local`, promoted to `compiler` via direct SQL since local signups start `pending`) to test the signed-in path, and confirmed anonymous gating in a signed-out state. Also had to run `npm run seed:bible` once against the freshly-reset local volume — Bible text is a one-time seed script, not part of any migration, so a fresh `db:reset` leaves `bible_verses` empty.

**2026-08-26 — Local Supabase port-collision fix**
- Restored via `/remember restore`; confirmed no drift beyond the already-known unrelated `supabase/config.toml` (`experimental.pgdelta`) change.
- Investigated the open "`db:start`/`db:reset` fails on a fresh volume" issue starting from a lead about a prior Oykon-side project-name collision. Reproduced live under Colima and found the real cause: this project's local ports overlap Docker Desktop's concurrently-running `oykon-local` project's ports, and Docker Desktop wins the host socket — confirmed via `lsof` and a PostgREST version mismatch in the response headers.
- Fixed by moving this project's local dev ports to a dedicated block in `supabase/config.toml`; verified clean on a genuinely fresh volume (`db:start`, `db:reset`, `db:verify-contract`, `tsc`, `npm test`, `next build`). Full detail in the Completed entry above.
- Kept the pre-existing, unexplained `experimental.pgdelta` drift out of this commit — its own change, unrelated to today's fix, left exactly as found for a separate decision later.

**2026-08-26 — Contribution policy and repository documentation organization**
- Adopted a project-specific `CONTRIBUTING.md` derived from Banka's useful conventions, with commit messages written from the author's perspective and AI help recorded only through `Assisted-by: <provider or product> (<model, if known>)` trailers.
- Added an ignored `dev/` convention for non-permanent local working material. Durable decisions and session state still belong in `context/`; completed audits and historical reports belong in `docs/history/`, not `dev/`.
- Audited the repository root and `docs/`: retained required root config and origin files; moved the completed Banka adoption assessment and dated backups into dedicated folders; renamed the retained Vesper source image descriptively; retired resolved feedback and obsolete reference artifacts. Kept local copies of still-useful working artifacts in ignored `dev/`.
- Updated current references to match the new paths and preserved immutable overflow content unchanged. The only unrelated worktree change observed during this batch is `supabase/config.toml`, which remains outside this documentation work.

**2026-08-25 — BA-007 shipped, Banka §2.9 adopted upstream, 2 of 5 webapp issues fixed**
- Committed and pushed BA-007's Vesper Discourse corrections (`c321a7b`/`870263f`) — closed out the entire BA-family stabilization backlog from the prior session.
- Ran `linis` on the progress-tracker archive split from 2026-08-24, then used this project's own bloat problem to propose a native Banka mechanism. That proposal was adopted upstream the same day as Banka Protocol §2.9 (Session-State Bloat Prevention and Correction) — this project's ad hoc archive split was migrated to §2.9's canonical `context/overflow/session-notes/` shape (commit `6c1c3a0`). The resolved feedback report was later retired from tracked project documentation.
- Reviewed Banka's `CONTRIBUTING.md`/`RELEASE-GOVERNANCE.md` for reuse. Adopted the `Assisted-by: Anthropic Claude Code (Sonnet 5)` commit-attribution convention (replacing `Co-Authored-By`); deliberately did not adopt release governance (no SemVer product version or published consumers here, unlike Banka's own multi-repo ecosystem). See `AGENTS.md` and Decisions Made.
- 5 direct-observation webapp issues were reported (see "Webapp issue audit" section above for full detail). Investigated all 5 against the code before any fix. Fixed #1 (Compiler placement bug, `b396178`/`724ca8b`) and #2 (Scripture Library sort bug, `2d1e3d7`) via `dredge` → confirm → targeted fix → `survey` → (issue #1 also got a `moor` capture, `2a06ee2`). Confirmed #3 is deliberate design, not a bug. #4 and #5 are real, confirmed gaps, not yet fixed.
- **Real environment problem found and only partially diagnosed, not resolved**: local `npm run db:start`/`db:reset` fails on a genuinely fresh Docker volume — ruled out collision with an unrelated local Supabase project, stale data, the migration file itself, and the `pgdelta` experimental flag (disabled, no change) — root cause still unknown. Worked around by testing extracted pure functions directly instead of a running app for both fixes tonight. This blocks live end-to-end verification until someone finds the actual cause — worth a dedicated session.
- Added a permission allowlist to `.claude/settings.json` (read-only `npm test`/`npm run lint`/`npx eslint`/`npm run db:status`) based on this session's actual command usage.
- Ended by adding a codified rule to `code-standards.md` (placement vs. editing of a shared Library entry are separate authorized operations) so issue #1's bug class can't recur independently in a future item type's picker.

**2026-08-24 — Post-adoption backlog audit and prioritization**
- First working session after Banka adoption (commit `f0aa3bf`). Restored via `/remember restore`, confirmed no drift (`main` clean, matches `origin/main`).
- Built a ranked build list from `build-plan.md`'s remaining v2/v3 items plus `docs/history/ADOPTION-ASSESSMENT.md`'s stabilization backlog (BA-003 through BA-008).
- Re-verified every BA-00x finding live rather than trusting the assessment's dates: `npm audit` (5 high-severity, Next.js-rooted, still open — BA-005), `npm run lint` (same 1 error + 3 warnings — BA-006), grepped `lib/liturgy/sectionItems.ts` (non-atomic `count ?? 0` insert confirmed — BA-003), grepped the export route (confirmed zero `try`/`catch` anywhere — BA-004). `npm ls` confirmed no missing/unmet dependencies; only version drift.
- Dependency audit: no missing packages. Outdated but low-urgency: React 19.2.4→19.2.8, Supabase JS 2.110→2.112, `@react-pdf/renderer` 4.5→4.8, and others — safe to batch whenever, not blocking anything.
- The backlog was ranked: BA-003/BA-004 first, then the rest of the high-priority list in order (see Up Next), with an explicit standing instruction to bury BA-008 cold rather than keep resurfacing it in status reports.
- Next actual build session should start on BA-003 (`section_items.position` atomic insert + DB uniqueness constraint).
- **Update, same day**: BA-003/BA-004/BA-005 all fully closed — migration run and confirmed live in Production (the `pg_constraint`/`pg_trigger` check was run personally), app code split into 4 clean commits via GitHub Desktop's line-level staging (`2b27fa6`, `b02fde5`, `e7c5d55`, `2a54664`, plus doc commit `aa44f2f`), all pushed to `origin/main` and confirmed matching. Vercel auto-deploys from `main`. **Next up: BA-007** — confirm the automated Vesper "Matthew 5–7" rotation reading against the printed handbook; this needs a manual check, not something verifiable from code.

**2026-08-15 — Integrity stabilization Phase 0 started**
- Preserved the uncommitted Speed Insights integration (`app/layout.tsx`, `package.json`, lockfile); confirmed Vercel Hobby and kept telemetry non-gating.
- Added free development tooling: `supabase@2.114.0` and `vitest@4.1.10`; added local database and test scripts. `npm test` passes its first two focused citation-format regression checks.
- Added `.env.local` preflight with three required variables and corrected `.env.local.example`, README, and code standards. The checker was verified both failing clearly with no env and succeeding with safe placeholders. An ignored `.env.local` now points only to the disposable local Supabase instance; no production secret is stored.
- Initialized `supabase/config.toml`, disabled unused local Storage/Realtime/Edge/Analytics services, set local password minimum to eight, and removed the two production Auth UUIDs from `20260725030000_user_roles.sql` so a clean environment can reach later migrations.
- Read-only production baseline: the homepage and four existing Morning liturgies load; the latest compile view shows real content; its Guide and Bulletin DOCX endpoints return HTTP 200 with non-empty documents (58,504 and 56,079 bytes); the public Web View is nav-free and omits leader-only cues. No Vesper liturgy currently exists to capture. No live control was clicked and no production data changed.
- Installed the free Colima runtime plus Docker CLI through Homebrew. `supabase start` built a blank database and `supabase db reset` rebuilt it; all 32 repository migrations applied in order on both runs. This closes the clean-replay blocker without touching production.
- Verification: environment preflight clean, targeted lint clean, TypeScript clean, focused tests clean, and production build clean with the local configuration and allowed font fetching. Full lint still has the two pre-existing errors recorded under Known Issues.
- Dredge follow-up after backup verification: direct schema-dump comparison changed the diagnosis from missing migration bookkeeping to a Mode 3 foundation mismatch. Production has hosted `rls_auto_enable` behavior, broad API grants, and no `notifications` table; clean local replay has `notifications` but no RLS on six core tables and `service_role` cannot select `liturgies`. The migrations record table shape but do not fully own the security/runtime contract. Stop condition honored: no schema, ledger, or production repair was applied pending approval of a revised approach.
- Revised approach approved and implemented locally: added `20260815010000_explicit_database_contract.sql` plus a hostname-locked `npm run db:verify-contract` check. A blank reset applied all 33 migrations. All 12 application tables report RLS enabled; only `authenticated → SELECT user_roles` and full `service_role` application-table privileges remain; only `service_role` can execute `create_liturgy()`. Behavioral verification passed for allowed and denied roles, public Home/Library/Reader routes returned 200, signed-out `/liturgy/new` showed its login guard, focused tests passed, and the production build passed. Full ESLint still reports the same two pre-existing errors. Production remains untouched; exact ledger/dry-run/live verification steps are recorded in `docs/PHASE-0-DATABASE-RECONCILIATION.md`.
- Production reconciliation completed after separate ledger and migration approvals. A fresh owner-only pre-change backup was verified; its schema/roles matched the earlier checkpoint exactly. The 32 historical versions were recorded, the dry run listed only the contract migration, and the contract migration was applied. Post-change schema inspection and ledger verification passed. Live Home, Library, Reader, Compile View, Web View, signed-out creation guard, Leader Guide DOCX, and Bulletin DOCX all passed. Remaining owner dependency: sign in with the existing account and confirm the account menu/Compile View, without sharing credentials.
- The password-bound live checks (sign-in, role, Compile View, sign-out, public homepage) were completed. Phase 0 closed.

**2026-08-16 — Integrity stabilization Phase 1 local containment batch**
- Re-audited every `"use server"` module and every liturgy mutation entry point. Existing create/delete/add/edit/remove/sermon/cue/end-note actions already used `getCurrentUser()`. The remaining direct gaps were `setShowPrayerGuide`, `setColumnBreak`, and `chooseVesperReading`; each now rejects anonymous/pending users before any service-role read or write.
- `translationPairing.ts` and `companionTranslation.ts` are internal helpers called only by already-authorized Server Actions. Replaced their `"use server"` directives with `server-only`, removing unnecessary client-callable endpoints without changing their internal behavior.
- Prayer Guide reference text remains public, but its “Add this Prayer Guide to the Leader’s Guide” mutation checkbox now renders only for a trusted Curator/Compiler. This preserves anonymous reading while removing a misleading control.
- Added a Vitest alias config plus four focused authorization tests. Anonymous Prayer Guide/column/Vesper calls are proven to stop before privileged access; a Compiler path is proven to reach the intended operations. Full suite: 6 passing tests. Changed files lint clean; full lint now has only the pre-existing `ReaderClient.tsx` effect error and three warnings. Production build succeeds. No Phase 1 code has been deployed.
- The session was paused after the local containment batch passed. Nothing from Phase 1 was staged, committed, pushed, or deployed. The larger working tree also contains the completed Phase 0 foundation, Vercel Speed Insights integration, local tooling, documentation, and the already-applied database contract migration; preserve all of it. The next session must restore first, confirm Git reality, and wait for explicit confirmation before choosing commit boundaries or touching Production.
