# Migrated historical decision 0011

2026-09-07** — Adopted a rewritten `RELEASE-GOVERNANCE.md` (via `charter`), reversing the 2026-08-25 decision above once the project owner decided it was time to start versioning properly. Key adaptations from Banka's own file: one version axis, not two (no "state-schema version" analog — this project generates nothing for another repository to migrate against); versions are applied retroactively to already-deployed commits, never gating a deploy; "established consumer" (the Major-bump trigger) scoped to exactly one real thing, the n8n Liturgy Automation's `app/api/automation/*` contract — nothing else in this app has a party programmatically depending on a stable shape; the existing informal `"v1"`/`"v1.1"`/`"v2"`/`"v3"` phase labels in `project-overview.md`/`architecture.md`/`build-plan.md` are explicitly a separate vocabulary from the new real SemVer axis, not renumbered or replaced. Release notes live in a new `CHANGELOG.md`, not folded into this file. `package.json` bumped from its long-static `0.1.0` to `0.5.0`, backed by a retrospective classification in `RELEASE-GOVERNANCE.md` mapping five real past milestones (v1 done, Banka adoption, Track A, Track B, this week's migration-integrity pass) to `0.1.0`–`0.5.0` — documentation only, no tags created for the past. `1.0.0` is deliberately reserved for a future point once the tagging practice itself has proven out over a few real releases, not backdated onto any commit.
